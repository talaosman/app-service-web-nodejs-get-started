doctype html
html
  head
    title My Clinic Portal
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")

    // Icons & font
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css")
    link(href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet")

    // Navbar + global layout styles
    style.
      body { margin: 0; font-family: 'Roboto', sans-serif; background:#f5f5f5; }

      nav.navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem 2rem;
        background: #1d3557;
        color: #fff;
        position: sticky;
        top: 0;
        z-index: 100;
      }

      nav.navbar .navbar-brand {
        font-size: 1.5rem;
        font-weight: bold;
        text-decoration: none;
        color: #fff;
      }

      ul.nav-links {
        list-style: none;
        display: flex;
        gap: 1.5rem;
        margin: 0;
        padding: 0;
      }

      ul.nav-links li a {
        text-decoration: none;
        color: #f1faee;
        font-weight: 500;
        padding: .4rem .8rem;
        border-radius: 6px;
        transition: .3s;
      }

      ul.nav-links li a:hover {
        background: #457b9d;
      }

      footer {
        margin-top: 4rem;
        padding: 1.5rem;
        text-align: center;
        background: #1d3557;
        color: white;
      }

  body
    nav.navbar
      a.navbar-brand(href="/") My Clinic Portal
      ul.nav-links
        li: a(href="/") Home
        li: a(href="/about") About
        li: a(href="/contact") Contact
        li: a(href="/login") Login
        li: a(href="/signup") Signup

    main
      block content

    footer
      p © #{new Date().getFullYear()} My Clinic Portal. All rights reserved.
